NEXA 3D WORKER LOCAL v1.0.2 — UI SMOKE FIX VALIDATION

Target: Electron / Windows x64
OpenAI: Not used
Worker protocol: Nexa 3D Studio Update 8 Web

FIXED      package.json — ui:smoke now runs the harness with Node
FIXED      scripts/ui-smoke.js — resolves Electron executable path as a string before spawn
UPDATED    nexa.project.json — application version 1.0.2
UPDATED    src/backend/nexa-worker.js — worker version 1.0.2
UPDATED    src/index.html — displayed version 1.0.2

PASS  JavaScript syntax
PASS  Delivery validator
PASS  Project isolation validator
PASS  Node baseline tests 3/3
PASS  ui:smoke command references local JavaScript harness
PASS  Electron spawn executable contract is string-validated
PASS  Existing Windows workflow remains unchanged

Root cause fixed: the previous package script executed scripts/ui-smoke.js with Electron itself, so require('electron') returned the Electron API object and child_process.spawn received an object instead of an executable path. The harness now runs under Node, where require('electron') resolves to the Electron executable path.
