'use strict';
console.log('Quick Texture is now integrated into Nexa 3D Worker Local v1.3.0.');
console.log('Open Nexa 3D Worker Local and start the normal Worker. No separate Quick Texture console is required.');
process.exit(0);
